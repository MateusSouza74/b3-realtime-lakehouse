# Deixa o `docker compose logs spark-streaming` legivel: so WARN do Spark,
# preservando os prints de progresso do job.

rootLogger.level = warn
rootLogger.appenderRef.stdout.ref = console

appender.console.type = Console
appender.console.name = console
appender.console.target = SYSTEM_OUT
appender.console.layout.type = PatternLayout
appender.console.layout.pattern = %d{HH:mm:ss} %-5p %c{1}: %m%n

logger.spark.name = org.apache.spark
logger.spark.level = warn

logger.jetty.name = org.sparkproject.jetty
logger.jetty.level = error

logger.kafka.name = org.apache.kafka
logger.kafka.level = error

logger.delta.name = io.delta
logger.delta.level = warn
